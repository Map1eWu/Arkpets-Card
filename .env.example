# Claude 额度看板配置
# 复制本文件为 .env，填入你的 sessionKey

# 从浏览器获取步骤：
# 1. 在 Chrome/Safari 打开并登录 https://claude.ai
# 2. 打开 DevTools（F12 或 Cmd+Option+I）
# 3. 切到 Application 标签 → Cookies → https://claude.ai
# 4. 找到名为 sessionKey 的条目，复制 Value 列的值（以 sk-ant- 开头）
# 5. 粘贴到下方等号后面

CLAUDE_SESSION_KEY=sk-ant-在这里粘贴你的sessionKey

# 面板右下角显示的账户标识（可留空）
ACCOUNT_LABEL=you@example.com

# 可选：网络代理
# HTTPS_PROXY=http://127.0.0.1:7890

# GPU 监控：ssh 目标（user@host 或 ~/.ssh/config 中的别名），需已配置免密登录
# 支持逗号分隔最多 2 台：GPU_HOST=serverA,serverB
# GPU_HOST=myserver
